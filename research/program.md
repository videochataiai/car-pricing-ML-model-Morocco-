# Task
Optimize `train.py` to predict Used Car Prices in Morocco.

# Goal
Maximize the `SCORE` output (R^2).

# Tools
Use XGBoost, CatBoost, or RandomForest. 
Handle missing mileage data by imputing the median.
